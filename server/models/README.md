# Server Model Folder

This folder is used by the central server to store uploaded model releases.

YOLO `.pt` files are not included in this repository. On first setup, start the central server, open the Model Manager page, and upload the downloaded `.pt` model to create the first active release.

Uploaded model files are runtime artifacts and should not be committed.
